import { type Config } from 'tailwindcss'

const config: Config = {
  theme: {
    extend: {
      fontFamily: {
        "manrope" : ['Manrope', 'sans-serif'],
      },
    },
  },
}

export default config
